{
  "name": "top100",
  "private": true,
  "scripts": {
    "dev": "bunx vite dev",
    "build": "echo 'No build needed: Cloudflare serves source files directly.'",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "vite": "^8.0.16",
    "wrangler": "^4.100.0"
  }
}
